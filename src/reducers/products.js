import data from '../products.json';
const products = (state = data, action) => {
    switch (action.type) {      
      default:      
        return state
    }
  }
    
  export default products
  